{
  "name": "deducibles-lambda",
  "version": "1.0.0",
  "description": "AWS Lambda function for processing deducibles",
  "main": "src/deducibles/infrastructure/bootstrap/handler.ts",
  "scripts": {
    "build": "tsc",
    "deploy": "aws lambda update-function-code --function-name your-function-name --zip-file fileb://path-to-your-zip-file.zip",
    "test": "jest --coverage"
  },
  "dependencies": {
    "aws-sdk": "^2.1000.0"
  },
  "devDependencies": {
    "@nestjs/common": "10.4.16",
    "@types/aws-lambda": "^8.10.0",
    "@types/jest": "^30.0.0",
    "jest": "29.7.0",
    "jest-cucumber": "3.0.1",
    "ts-jest": "^29.4.5",
    "typescript": "^4.5.0"
  },
  "author": "Marlon Teran",
  "license": "MIT"
}
